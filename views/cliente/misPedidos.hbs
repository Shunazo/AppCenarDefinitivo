<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mis Pedidos - AppCenar</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">
    <link href="/assets/css/cliente/misPedidos.css" rel="stylesheet">
</head>
<body>
    <div class="d-flex">
        <!-- Sidebar -->
        <div class="sidebar">
            <div class="sidebar-header">
                <div class="user-info">
                    <img src="{{usuario.fotoPerfil}}" alt="Profile" class="user-avatar">
                    <span>{{usuario.nombre}}</span>
                </div>
            </div>

            <div class="sidebar-menu">
                <a href="/cliente/home" class="menu-item">
                    <i class="fas fa-home"></i>
                    <span>Inicio</span>
                </a>
                <a href="/cliente/perfil/edit" class="menu-item">
                    <i class="fas fa-user"></i>
                    <span>Mi Perfil</span>
                </a>
                <a href="/cliente/pedidos" class="menu-item active">
                    <i class="fas fa-receipt"></i>
                    <span>Mis Pedidos</span>
                </a>
                <a href="/cliente/direcciones" class="menu-item">
                    <i class="fas fa-map-marker-alt"></i>
                    <span>Mis Direcciones</span>
                </a>
                <a href="/cliente/favoritos" class="menu-item">
                    <i class="fas fa-heart"></i>
                    <span>Favoritos</span>
                </a>
                <button id="logout-btn" class="menu-item logout">
                    <i class="fas fa-sign-out-alt"></i>
                    <span>Cerrar Sesión</span>
                </button>
            </div>
        </div>
        <!-- Content -->
        <div id="content" class="p-4">
            <div class="container">
                <h2 class="mb-4"><i class="fas fa-shopping-bag me-2"></i>Mis Pedidos</h2>
                
                <div class="pedidos-list">
                    {{#each pedidos}}
                    <a href="/cliente/pedidos/{{id}}" class="pedido-card">
                        <div class="card mb-3">
                            <div class="row g-0">
                                <div class="col-md-2 d-flex align-items-center justify-content-center">
                                    <img src="{{comercio.logo}}" alt="{{comercio.nombreComercio}}" class="comercio-logo">
                                </div>
                                <div class="col-md-7">
                                    <div class="card-body">
                                        <h5 class="card-title">{{comercio.nombreComercio}}</h5>
                                        <div class="pedido-details">
                                            <span class="badge 
                                                {{#ifEquals estado 'pendiente'}}bg-warning{{/ifEquals}}
                                                {{#ifEquals estado 'en_proceso'}}bg-info{{/ifEquals}}
                                                {{#ifEquals estado 'completado'}}bg-success{{/ifEquals}}">
                                                {{#ifEquals estado 'pendiente'}}Pendiente{{/ifEquals}}
                                                {{#ifEquals estado 'en_proceso'}}En Proceso{{/ifEquals}}
                                                {{#ifEquals estado 'completado'}}Completado{{/ifEquals}}
                                            </span>
                                            <p class="productos-count">{{totalProductos}} productos</p>
                                            <p class="fecha-pedido">
                                                <i class="far fa-calendar-alt me-2"></i>
                                                {{fechaHora}} 
                                            </p>
                                        </div>
                                    </div>
                                </div>
                                <div class="col-md-3 d-flex align-items-center justify-content-end pe-4">
                                    <div class="precio-total">
                                        <span class="precio-label">Total</span>
                                        <span class="precio-valor">RD${{total}}</span>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </a>
                    {{else}}
                    <div class="alert alert-info text-center">
                        <i class="fas fa-info-circle me-2"></i>
                        No tienes pedidos realizados.
                    </div>
                    {{/each}}
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/assets/js/index.js"></script>
</body>
</html>

<!-- eta vaina e pa mostrar el listado de pedidos del comercio, tu modificalo como te de la gana 

<div class="container">
    <h1>Pedidos</h1>
    {{#if pedidos.length}}
        <ul class="list-group">
            {{#each pedidos}}
            <a href="/comercio/pedidos/{{id}}" class="list-group-item list-group-item-action d-flex align-items-center">
                <img src="{{comercio.logo}}" alt="{{comercio.nombreComercio}}" class="comercio-logo me-3" style="width: 50px; height: 50px;">
                <div class="w-100">
                    <div class="d-flex justify-content-between">
                        <h5 class="mb-1">{{comercio.nombreComercio}}</h5>
                        <small class="text-muted">{{fechaHora}}</small>
                    </div>
                    <p class="mb-1">
                        Estado: {{estado}} | Productos: {{totalProductos}} | Total: ${{total}}
                    </p>
                </div>
            </a>
            {{/each}}
        </ul>
    {{else}}
        <p class="text-center">No existen pedidos actualmente.</p>
    {{/if}}
</div>


-->
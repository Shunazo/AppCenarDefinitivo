<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mantenimiento de Productos - {{comercio.nombreComercio}}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">
    <link href="/assets/css/mantenimiento-productos.css" rel="stylesheet">
    <link href="/assets/css/mantenimiento-categorias.css" rel="stylesheet">

</head>
<body>
    <div class="wrapper">
        <!-- Sidebar -->
        <nav id="sidebar" class="bg-dark text-white">
            <div class="sidebar-header">
                <img src="{{comercio.logo}}" alt="Logo" class="logo-img mb-3">
                <h4>{{comercio.nombreComercio}}</h4>
            </div>
            
            <ul class="list-unstyled components">
                <li><a href="/comercio/home"><i class="fas fa-home me-2"></i>Home</a></li>
                <li><a href="/comercio/perfil"><i class="fas fa-user me-2"></i>Mi Perfil</a></li>
                <li><a href="/comercio/categorias"><i class="fas fa-tags me-2"></i>Categorías</a></li>
                <li class="active">
                    <a href="/comercio/productos"><i class="fas fa-box me-2"></i>Productos</a>
                </li>
                <li><a href="/logout"><i class="fas fa-sign-out-alt me-2"></i>Cerrar Sesión</a></li>
            </ul>
        </nav>

        <!-- Content -->
        <div id="content" class="p-4">
            <div class="container">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <h2><i class="fas fa-box me-2"></i>Productos</h2>
                    <a href="/comercio/productos/create" class="btn btn-primary">
                        <i class="fas fa-plus me-2"></i>Nuevo Producto
                    </a>
                </div>

                {{#if productos.length}}
                    <div class="row">
                        {{#each productos}}
                        <div class="col-md-6 col-lg-4 mb-4">
                            <div class="card producto-card">
                                <img src="{{imagen}}" class="card-img-top producto-imagen" alt="{{nombre}}">
                                <div class="card-body">
                                    <h5 class="card-title">{{nombre}}</h5>
                                    <p class="card-text descripcion">{{descripcion}}</p>
                                    <div class="categoria-badge">
                                        <span class="badge bg-info">{{categoria.nombre}}</span>
                                    </div>
                                    <div class="precio">
                                        <span class="precio-valor">${{precio}}</span>
                                    </div>
                                    <div class="acciones mt-3">
                                        <a href="/comercio/productos/edit/{{id}}" 
                                           class="btn btn-warning btn-sm">
                                            <i class="fas fa-edit"></i> Editar
                                        </a>
                                        <button type="button"
                                                class="btn btn-danger btn-sm"
                                                data-bs-toggle="modal"
                                                data-bs-target="#deleteModal{{id}}">
                                            <i class="fas fa-trash"></i> Eliminar
                                        </button>
                                    </div>
                                </div>
                            </div>

                            <!-- Delete Modal -->
                            <div class="modal fade" id="deleteModal{{id}}" tabindex="-1">
                                <div class="modal-dialog">
                                    <div class="modal-content">
                                        <div class="modal-header">
                                            <h5 class="modal-title">Confirmar Eliminación</h5>
                                            <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                                        </div>
                                        <div class="modal-body">
                                            <p>¿Está seguro que desea eliminar este producto?</p>
                                        </div>
                                        <div class="modal-footer">
                                            <button type="button" 
                                                    class="btn btn-secondary" 
                                                    data-bs-dismiss="modal">Cancelar</button>
                                            <form action="/comercio/productos/delete/{{id}}" 
                                                  method="POST" 
                                                  class="d-inline">
                                                <button type="submit" 
                                                        class="btn btn-danger">Eliminar</button>
                                            </form>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        {{/each}}
                    </div>
                {{else}}
                    <div class="alert alert-info">
                        <i class="fas fa-info-circle me-2"></i>
                        No hay productos registrados. ¡Crea uno nuevo!
                    </div>
                {{/if}}
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

  <!DOCTYPE html>
  <html lang="es">
  <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>Mis Direcciones - AppCenar</title>
      <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
      <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">
      <link href="/assets/css/direcciones.css" rel="stylesheet">
  </head>
  <body>
      <div class="container mt-4">
          <div class="d-flex justify-content-between align-items-center mb-4">
              <h2>Mis Direcciones</h2>
              <a href="/cliente/direcciones/create" class="btn btn-primary">
                  <i class="fas fa-plus"></i> Nueva Dirección
              </a>
          </div>

          <div class="row">
              {{#each direcciones}}
              <div class="col-md-6 mb-4">
                  <div class="card">
                      <div class="card-body">
                          <h5 class="card-title">{{nombre}}</h5>
                          <p class="card-text">{{descripcion}}</p>
                          <div class="btn-group">
                              <a href="/cliente/direcciones/edit/{{id}}" class="btn btn-warning">
                                  <i class="fas fa-edit"></i> Editar
                              </a>
                              <form method="POST" action="/cliente/direcciones/delete/{{id}}" data-entity-name="{{direccion.nombre}}" data-entity-type="Dirección">
                                <button type="button" onclick="confirmDelete(this)" class="btn btn-danger">
                                <i class="fas fa-trash"></i> Eliminar
                                </button>
                                </form>
                          </div>
                      </div>
                  </div>
              </div>
              {{/each}}
          </div>
      </div>

      <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
      <script src="../../assets/js/index.js"></script>
  </body>
  </html>

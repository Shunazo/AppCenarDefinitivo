<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{pageTitle}}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">
    <link href="/assets/css/admin/home-admin.css" rel="stylesheet">
</head>
<body>
    <div class="wrapper">
        <nav id="sidebar" class="bg-dark">
            <div class="sidebar-header">
                <h3><i class="fas fa-user-shield me-2"></i>Panel Admin</h3>
            </div>
           <ul class="list-unstyled components">
                <li class="active"><a href="/administrador/home"><i class="fas fa-home me-2"></i>Dashboard</a></li>
                <li><a href="/administrador/clientes"><i class="fas fa-users me-2"></i>Clientes</a></li>
                <li><a href="/administrador/deliveries"><i class="fas fa-motorcycle me-2"></i>Delivery</a></li>
                <li><a href="/administrador/comercios"><i class="fas fa-store me-2"></i>Comercios</a></li>
                <li><a href="/administrador/config"><i class="fas fa-cogs me-2"></i>Configuración</a></li>
                <li><a href="/administrador/administradores"><i class="fas fa-user-tie me-2"></i>Administradores</a></li>
                <li><a href="/administrador/tipo-comercio"><i class="fas fa-store-alt me-2"></i>Tipos de Comercio</a></li>
            </ul>
                <button id="logout-btn" class="btn-sidebar">
                    <i class="fas fa-sign-out-alt me-2"></i>Cerrar Sesión
                </button>
        </nav>

        <!-- Page Content -->
        <div id="content">
            <div class="container-fluid">
                <h1 class="mb-4">{{pageTitle}}</h1>

                <!-- Create Button -->
                <a href="/administrador/tipo-comercio/create" class="btn btn-primary mb-4">Crear Nuevo Tipo de Comercio</a>

                <div class="row">
                    {{#each tipocomercios as |tipo|}}
                        <div class="col-md-6 col-lg-4 mb-4">
                            <div class="card stat-card">
                                <div class="card-body">
                                    <h5 class="card-title">{{tipo.nombre}}</h5>
                                    <img src="{{tipo.icono}}" alt="{{tipo.nombre}}" class="img-fluid">
                                    <p>{{tipo.descripcion}}</p>
                                    <p>Cantidad de Comercios: {{tipo.comerciosCount}}</p>

                                    <!-- Edit and Delete Buttons -->
                                    <a href="/administrador/tipo-comercio/edit/{{tipo.id}}" class="btn btn-warning">Editar</a>
                                    <form method="POST" action="/administrador/tipo-comercio/delete/{{id}}" data-entity-name="{{tipocomercio.nombre}}" data-entity-type="Tipo de Comercio">
                                <button type="button" onclick="confirmDelete(this)" class="btn btn-danger">
                                <i class="fas fa-trash"></i> Eliminar
                                </button>
                                </form>
                                </div>
                            </div>
                        </div>
                    {{/each}}
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="/assets/js/index.js"></script>
</body>
</html>

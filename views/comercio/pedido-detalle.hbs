<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalles del Pedido - AppCenar</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">
    <link href="/assets/css/comercio/comercio.css" rel="stylesheet">
</head>
<body>
    <div class="wrapper">
        <!-- Sidebar Navigation -->
        <nav id="sidebar" class="bg-dark text-white">
            <div class="sidebar-header">
                <img src="{{comercio.logo}}" alt="Logo Comercio" class="rounded-circle mb-3" style="width: 80px; height: 80px;">
                <h4 class="text-white">{{comercio.nombreComercio}}</h4>
            </div>
            
            <ul class="list-unstyled components">
                <li>
                    <a href="/comercio/home" class="nav-link text-white">
                        <i class="fas fa-home me-2"></i>Home
                    </a>
                </li>
                <li>
                    <a href="/comercio/perfil/edit" class="nav-link text-white">
                        <i class="fas fa-user me-2"></i>Perfil
                    </a>
                </li>
                <li>
                    <a href="/comercio/categorias" class="nav-link text-white">
                        <i class="fas fa-tags me-2"></i>Categorías
                    </a>
                </li>
                <li>
                    <a href="/comercio/productos" class="nav-link text-white">
                        <i class="fas fa-box me-2"></i>Productos
                    </a>
                </li>
                <li>
                    <button id="logout-btn" class="nav-link text-white logout-btn">
                        <i class="fas fa-sign-out-alt me-2"></i>Cerrar Sesión
                    </button>
                </li>
            </ul>
        </nav>

        <!-- Main Content -->
        <div id="content" class="p-4">
            <div class="container">
                <h1>Detalles del Pedido</h1>
                <div class="pedido-detail">
                    <img src="{{pedido.comercio.logo}}" alt="{{pedido.comercio.nombreComercio}}" class="comercio-logo mb-3">
                    <h2>{{pedido.comercio.nombreComercio}}</h2>
                    <p><strong>Estado:</strong> {{pedido.estado}}</p>
                    <p><strong>Fecha y Hora:</strong> {{pedido.fechaHora}}</p>

                    <h3>Productos</h3>
                    <ul>
                        {{#each pedido.productosPedido}}
                        <li>
                            <img src="/images/{{producto.imagen}}" alt="{{producto.nombre}}" class="product-image me-2" style="width: 80px; height: 80px;">
                            <span><strong>{{producto.nombre}}</strong> - ${{producto.precio}}</span>
                            <span>Cantidad: {{cantidad}}</span>
                        </li>
                        {{/each}}
                    </ul>

                    <h3><strong>Total:</strong>RD${{pedido.total}}</h3>

                    {{#ifEquals pedido.estado 'pendiente'}}
                    <form action="/comercio/pedidos/assign/{{pedido.id}}" method="POST">
                        <button type="submit" class="btn btn-primary">Asignar Delivery</button>
                    </form>
                    {{/ifEquals}}

                    <a href="/comercio" class="btn btn-secondary mt-3">Volver al Home</a>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="../../assets/js/index.js"></script>
</body>
</html>

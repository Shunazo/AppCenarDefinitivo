
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mis Favoritos - AppCenar</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">
    <link href="/assets/css/misFavoritos.css" rel="stylesheet">
</head>
<body>
    <div class="d-flex">
        <!-- Sidebar -->
        <div class="sidebar">
            <div class="sidebar-header">
                <div class="user-info">
                    {{#if usuario.fotoPerfil}}
                        <img src="{{usuario.fotoPerfil}}" alt="Profile" class="user-avatar">
                    {{else}}
                        <i class="fas fa-user-circle fa-4x profile-icon"></i>
                    {{/if}}
                    <h4 class="user-name">{{usuario.nombre}}</h4>
                </div>
            </div>

            <div class="sidebar-menu">
                <a href="/cliente/home" class="menu-item">
                    <i class="fas fa-home"></i>
                    <span>Inicio</span>
                </a>
                <a href="/cliente/perfil/edit" class="menu-item">
                    <i class="fas fa-user"></i>
                    <span>Mi Perfil</span>
                </a>
                <a href="/cliente/pedidos" class="menu-item">
                    <i class="fas fa-receipt"></i>
                    <span>Mis Pedidos</span>
                </a>
                <a href="/cliente/direcciones" class="menu-item">
                    <i class="fas fa-map-marker-alt"></i>
                    <span>Mis Direcciones</span>
                </a>
                <a href="/cliente/favoritos" class="menu-item active">
                    <i class="fas fa-heart"></i>
                    <span>Favoritos</span>
                </a>
                <button id="logout-btn" class="menu-item logout">
                    <i class="fas fa-sign-out-alt"></i>
                    <div class="logout-text">Cerrar Sesión</div>
                </button>
            </div>
        </div>

        <!-- Main Content -->
        <div class="main-content">
            <div class="container">
                <div class="header-section">
                    <h2><i class="fas fa-heart me-2"></i>Mis Favoritos</h2>
                </div>

                <div class="favoritos-list">
                    {{#if favoritos.length}}
                        <div class="row g-4">
                            {{#each favoritos}}
                                <div class="col-md-6 col-lg-4">
                                    <div class="favorito-card">
                                        <a href="/cliente/comercio/{{comercio.id}}" class="comercio-link">
                                            <div class="card">
                                                <div class="card-body">
                                                    <div class="comercio-logo">
                                                        <img src="{{comercio.logo}}" alt="{{comercio.nombre}}">
                                                    </div>
                                                    <h5 class="comercio-nombre">{{comercio.nombre}}</h5>
                                                    <button class="btn-remove-favorite" data-id="{{id}}">
                                                        <i class="fas fa-heart-broken"></i>
                                                        Eliminar de favoritos
                                                    </button>
                                                </div>
                                            </div>
                                        </a>
                                    </div>
                                </div>
                            {{/each}}
                        </div>
                    {{else}}
                        <div class="alert alert-info text-center">
                            <i class="fas fa-info-circle me-2"></i>
                            No tienes comercios favoritos. ¡Explora y agrega tus comercios preferidos!
                        </div>
                    {{/if}}
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/assets/js/index.js"></script>
</body>
</html>
